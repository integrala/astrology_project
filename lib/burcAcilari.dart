//ignore: file_names

import 'package:flutter/material.dart';

class BurcAcilar extends StatelessWidget {
  const BurcAcilar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("ASTROLOJİDE AÇILAR"),
      ),
    );
  }
}
